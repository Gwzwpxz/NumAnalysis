function [x] = sor(A, b, maxiter, tol)
% Numerical Analysis Review. Chapter 1 test
% Gwz, Shanghai University of Finance and Economics

L = tril(A);
U = triu(A);
d = diag(A);
D = diag(d);
DwL = D - w * L;
DwL2 = D - 

M1 = DwL \ ((1 - w) * D + w * U);
g = w * (DwL \ b);

n = length(d);
x = zeros(n, 1);

for i = 1:maxiter
    
    if norm(A * x - b) / norm(b) < tol
        break;
    end % End if
    
    x1 = M1 * x + g;
    x = 
    
end % End for

end % End function